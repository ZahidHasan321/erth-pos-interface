# Implementation map (NON-AUTHORITATIVE — may drift)

> Extracted from the spec. Navigation aid only — where code lives, not what it must do. If anything here disagrees with the spec (`SPEC.md` §1–§6) or the working rules (`ENGINEERING.md` §7–§9, §11–§12), **the spec wins and this is stale.** Never cite this as the source of a test's expected value.

- **Schema & types:** `packages/database/src/schema.ts`, `.../utils.ts` (`isAlteration`, `getAlterationNumber`, `evaluateBrovaFeedback`, `getShowroomStatus`).
- **Lifecycle RPCs/triggers:** `packages/database/src/triggers.sql` — `save_work_order_garments`, `complete_work_order`, `record_payment_transaction`, `record_bulk_payment` (§3 atomic bulk cashier payment), `cashier_confirm_orders_no_payment` (§3 confirm-without-payment), `get_cashier_pending_orders` (§3 Pending queue), `toggle_home_delivery`, `collect_garments`, `dispatch_order` (WORK cashier-processed gate, §3), `receive_garments`, `dispatch_garments_to_shop`, `release_finals`, `create_replacement_garment` (shop-initiated redo, §2.5), `resume_parked_redo` (shop dispatch un-park), `redo_promote_final_to_brova` (redo outcome 3), `create_complete_sales_order`, `recompute_order_phase`, `open`/`close`/`reopen_register`, `can_access_brand`, the `stock_movements` triggers.
- **Spec-as-oracle suite:** `packages/database/src/__tests__/workflow*.test.ts` + driver/fixtures in `packages/database/scripts/lifecycle/`; config `vitest.workflow.config.ts`.
- **Shop:** `apps/pos-interface/src` — `api/`, `hooks/`, `routes/` (`$main/`, `cashier/`), `components/forms|order-management|orders-at-showroom|cashier`, `context/auth.tsx`, `lib/constants.ts` (`BRANDS_WITH_CASHIER`, `brandUsesCashier`), `index.css`.
- **Workshop:** `apps/workshop/src` — `routes/(main)/assigned` (order labels + linked-order clustering, §2.13), `routes/(main)/dispatch.tsx` (Accept-with-Fix + linked-sibling stranding confirmations, §2.4·6/§2.13), `components/shared/PageShell.tsx` + `StageBadge.tsx`, `lib/production-logic.ts`, `lib/qc-spec.ts`, `lib/utils.ts` (date helpers), `index.css` (typography role table).
- **Order linking (§2.13):** schema `work_orders.linked_order_id`/`linked_date`/`unlinked_date` (`schema.ts`); shop UI `apps/pos-interface/src/components/order-management/{link,unlink}-order.tsx` + `api/orders.ts` (`getOrdersForLinking`, `getLinkedOrders`, `updateOrder`); workshop visibility via `get_assigned_orders_page` (`linked_order_id`/`link_group_id` + group-adjacent ordering, `triggers.sql`) and `WORKSHOP_QUERY`'s `linked_order_id` join (`apps/workshop/src/api/garments.ts`).
- **Cashier processing gate (§3):** marker `work_orders.cashier_processed_at`/`cashier_processed_by` (`schema.ts`, migration `0027_cashier_processing_fields.sql`); set by `record_payment_transaction` (first WORK payment), `cashier_confirm_orders_no_payment`, and `record_bulk_payment`; enforced in `dispatch_order` + hidden in the dispatch queue (`api/orders.ts` `getOrdersForDispatch`). Shop UI: Pending queue + bulk-payment page `apps/pos-interface/src/routes/cashier/` (`index.tsx`, `process.tsx`, `orders.tsx`) + `components/cashier/` (pending list, bulk-payment) + `api/cashier.ts` (`getCashierPendingOrders`, `recordBulkPayment`, `cashierConfirmNoPayment`) + `hooks/useCashier.ts`. Advance persisted for ERTH in `routes/$main/orders/new-work-order.tsx`.
- **QC defect attribution (§6):** type `QcDefectAttribution` + `QcAttempt.defect_attributions` (`schema.ts`); capture in the QC fail dialog's "Caused by" column (`apps/workshop/src/components/terminals/QualityCheckForm.tsx` — `buildQcContributors`/`CausedBySelect`), threaded `submitQc` → `buildQcAttempt` (`api/garments.ts`, `lib/production-logic.ts`); aggregation `get_qc_analytics.attributed_defects` (`triggers.sql`) surfaced by `routes/(main)/qc-analytics.tsx` (`TeamDefectBreakdown`) via `api/qcAnalytics.ts` (`AttributedDefect`).
