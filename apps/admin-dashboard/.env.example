# Admin Dashboard — reads the SAME Supabase project as the shop and workshop apps.
# Copy to .env.local for local dev (both are gitignored). In production these are
# set as Vercel environment variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
